name: Test Basic Execution

on:
  workflow_dispatch:  # فقط دستی

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Start
        run: echo "🚀 شروع تست..."
        
      - name: Step 2 - Check time
        run: |
          echo "🕐 زمان UTC: $(date -u)"
          echo "🇮🇷 زمان ایران: $(TZ=Asia/Tehran date)"
          
      - name: Step 3 - Simple command
        run: echo "✅ این یک دستور ساده است"
        
      - name: Step 4 - Finish
        run: echo "🏁 تست کامل شد!"
