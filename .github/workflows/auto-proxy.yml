name: Update Proxies Only

on:
  schedule:
    - cron: '*/5 * * * *'  # هر ۵ دقیقه فقط آپدیت
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📥 دریافت کد
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: 🐍 نصب پایتون
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: 📦 نصب کتابخانه
        run: pip install requests
        
      - name: 🔄 آپدیت پروکسی‌ها
        run: python update_proxies.py
        
      - name: 💾 ذخیره تغییرات
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add texts.txt
          
          # فقط اگه تغییری وجود داشت، commit و push کن
          if ! git diff --staged --quiet; then
            git commit -m "🔄 آپدیت پروکسی $(date +'%H:%M')"
            git push
            echo "✅ پروکسی‌ها آپدیت و ذخیره شدند"
          else
            echo "📭 پروکسی جدیدی پیدا نشد"
          fi
